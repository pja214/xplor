<h1>Dislikes</h1>

<% if @blogs.count > 0 %>
  <table>
    <thead>
      <th>Blogs</th>
    </thead>
    <tbody>
      <tr>
        <% @blogs.each do |blog| %>
        <td><%= link_to blog_name, blog_url %></td>
        <% end %>
      </tr>
    </tbody>
  </table>
  <% else %>
    <p>
      Were you recommended a blog that you didn't like? Dislike it so we can improve your recommendations.
    </p>
  <% end %>