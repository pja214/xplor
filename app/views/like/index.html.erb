<h1>Likes</h1>

<% if @blogs.count > 0 %>
  <table>
    <thead>
      <th>Blogs</th>
    </thead>
    <tbody>
      <tr>
        <% @blogs.each do |blog| %>
        <td><%= link_to blog_name, blog_url %></td>
        <% end %>
      </tr>
    </tbody>
  </table>
  <% else %>
    <p>
      Like some blogs to get better recommendations!
    </p>
  <% end %>